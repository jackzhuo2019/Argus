# Argus · 守夜人

> 一个开源的多 Agent 运维值班班组。它把「告警 → 根因 → 修复 → 验证 → 复盘」这条本该由人在凌晨三点完成的链路，交给一组职责分明、全程可审计、高风险动作必须请示人类的 AI Agent 来完成。

Argus 的定位是**「人类在环的自主运维闭环」**："零"指零日常重复性人力消耗（无人盯盘、无人工分诊告警风暴），而非零人类责任 / 零审批。重大事故（L3/L4、未知动作）仍由人拍板。目标是**把人的注意力从 100 起告警收敛到 3 起真正需要判断的决策**，让值班工程师从重复劳动中解放出来，把判断力留给真正重要的时刻。

---

## 项目定位

| 项 | 内容 |
|---|---|
| 项目类型 | 开源项目 + 可运行 Demo |
| 技术栈 | Kubernetes + Prometheus / Alertmanager / Grafana / Loki 自建可观测栈 |
| 协同框架 | AgentTeams（原 HiClaw），Apache-2.0 |
| 自治策略 | 分级自治（L0–L4 风险分层，高风险强制人工审批） |

---

## 为什么是多 Agent，而不是单个大模型

1. **职责边界即安全边界**：排障需要广泛读权限，修复需要收敛的写权限。拆成多 Agent 后，权限可按角色最小化授予。
2. **上下文隔离**：告警降噪需要处理海量低价值消息，根因分析需要深度推理。分 Agent = 分上下文，噪声不淹没推理。
3. **验证者必须独立于执行者**：让写出修复方案的同一个 Agent 判断「修好了没有」，等同于学生自己批卷子。恢复验证必须是独立职能、独立证据源。
4. **失败可局部降级**：根因定位失败不应阻断告警聚合；执行熔断不应影响复盘归档。

---

## 核心设计原则

| # | 原则 | 说明 |
|---|---|---|
| ① | 安全边界靠架构，不靠 Prompt | 凭证分两平面：LLM/MCP 访问凭据由 Higress AI 网关代持；**K8s 执行凭据由独立 Broker 持有**，按需 mint 短期按 SA 最小化令牌，Agent 结构性不持真实集群凭据。即使 Agent 被提示注入攻破，破坏上限由 Broker + RBAC 硬约束 |
| ② | 执行者与验证者强制分离 | Remediator 负责变更，Validator 独立取数验收；Validator 不可复用 Remediator 的数据作为依据，杜绝自证 |
| ③ | 风险等级由独立引擎裁决 | 动作风险等级由独立的风险引擎判定，而非 Agent 自评，避免「为闭环而闭环」 |

---

## 系统架构

### Agent 编队

- **1 个 Manager**：`IncidentCommander` 事件指挥官，负责编排与状态机推进
- **5 个 Worker**：
  - `Sentinel` 哨兵 —— 多源告警聚合与降噪
  - `Diagnostician` 诊断官 —— 根因定位与证据链
  - `Remediator` 执刀手 —— 修复方案生成与执行（高风险需 Human 审批）
  - `Validator` 验收官 —— 服务恢复独立验证，不通过触发回滚
  - `Scribe` 书记官 —— 事故复盘与知识沉淀
- **1 个 Human**：`OnCall` 值班人，保留 L3 及以上决策权

### 闭环状态机

每起事故由 `IncidentCommander` 拆解为 7 段子任务，每段有明确定义的输入契约、超时与失败分支：

```
triage → diagnose → plan → approve → execute → verify → review
 告警降噪   根因定位   方案生成   人工审批   执行变更   恢复验证   复盘沉淀
```

### 闭环流程

```text
多源告警 (Prometheus / Loki / K8s Event)
   │
   ▼
[ Sentinel 哨兵 ]         聚合 · 降噪 · 定级
   │
   ▼
{ IncidentCommander 事件指挥官 }
   │
   ▼
[ Diagnostician 诊断官 ]   根因定位 + 证据链
   │
   ▼
[ Remediator 执刀手 ]      方案生成 + 执行
   ├── L3+ 需审批 ──▶ ( Human OnCall 审批 ) ──▶ 回到执行
   │
   ▼
[ Validator 验收官 ]       独立取数验证
   ├── 通过 ──▶ [ Scribe 书记官 ] 复盘 + 知识沉淀 ──▶ 知识库（反哺 RAG）
   └── 失败 ──▶ 回滚请求 ──▶ 回到 Remediator
```

---

## 技术栈

| 层 | 选型 |
|---|---|
| 编排框架 | AgentTeams（Apache-2.0），Manager-Workers 架构，基于 Matrix 协议通信，K8s CRD 声明式定义 Worker/Team/Human |
| 可观测底座 | Kubernetes + Prometheus / Alertmanager / Grafana / Loki（自建，不重造） |
| 凭证安全 | **双平面**：Higress AI 网关代持 LLM/MCP 访问密钥（Worker 持 consumer token）；独立 K8s 凭证 Broker 持有真实集群凭据，按需 mint 短期 scoped 令牌供执行（Agent 不持真凭据） |
| 运行时 | OpenClaw（Node，技能生态丰富）/ QwenPaw（Python，轻量）/ Hermes（终端沙箱 + 持久记忆），可混用 |
| 能力层 | Nacos Skills Registry（17 个可复用 Skill，支持版本化/灰度/回滚）+ MinIO（Agent 间大对象共享）+ AgentLoop（可观测） |

---

## 目标与验收指标

| # | 目标 | 可衡量结果 |
|---|---|---|
| G1 | 多源告警收敛为少量高置信事件 | 降噪率 ≥ 90%，事件聚合准确率 ≥ 85% |
| G2 | 自动产出可信根因与证据链 | 根因 Top-1 ≥ 60%，Top-3 ≥ 85%，结论须附证据引用 |
| G3 | 安全边界内自动修复 | L1/L2 类故障自主闭环率 ≥ 70%，误修复率 ≤ 2% |
| G4 | 独立 Agent 验证，不达标回滚 | 验证覆盖率 100%，回滚成功率 100% |
| G5 | 事故自动沉淀为知识资产 | 复盘自动生成率 100%，蒸馏为 Runbook/Skill 转化率 ≥ 30% |
| G6 | 全流程可审计可追溯 | 每个动作有 Trace + 操作日志 + 变更快照，支持完整回放 |

**北极星指标**：自主闭环率（须与对称护栏绑定——误修复率 ≤ 2% + 过度保守率 ≤ 15%，且全部指标在固定评估数据集上计算，避免"冒进"或"永远不执行"两种退化）。

---

## 明确不做什么（Non-goals）

- ❌ 不做监控数据采集与存储（复用现有 Prometheus/Loki 栈）
- ❌ 不做告警规则的自动生成与调优
- ❌ 不做容量规划与成本优化
- ❌ 不做安全事件响应（SOC/SIEM）
- ❌ 不覆盖有状态服务的数据面修复（数据库主从切换等 v1.0 一律划入 L4 禁止区）
- ❌ 不做多云/混合云统一纳管（v1.0 仅保证 K8s + Prometheus 栈跑通）
- ❌ 不做自研 LLM 或模型微调（通过 Higress 网关统一接入）
- ❌ 不替代人工 On-Call 制度（Argus 是值班人的副驾）

---

## 目录结构

```
.
├── README.md
    └── docs/
        ├── PRD-Argus-零人工运维多Agent闭环系统.md   # 完整需求文档（18 章）
        └── 技术要点对照检查表.md                          # 技术要点对照与缺口清单
```

---

## 当前状态与路线图

项目已完成需求定义与架构设计，正在推进工程落地：

- [ ] 补全剩余 Skill 的九要素契约（共 17 个 Skill，部分已定义）
- [ ] 搭建可运行 Demo 环境（K8s + 自建可观测栈）
- [ ] 构建故障注入与验证数据集
- [ ] 推进若干待决议项：LLM 选型、观测方案选型、是否引入消息队列、Runbook 格式、观察期时长

---

## 文档索引

- 需求文档：[`docs/PRD-Argus-零人工运维多Agent闭环系统.md`](docs/PRD-Argus-零人工运维多Agent闭环系统.md)
- 技术要点对照：[`docs/技术要点对照检查表.md`](docs/技术要点对照检查表.md)

---

## 许可证

本项目基于 **Apache-2.0** 许可证开源。
